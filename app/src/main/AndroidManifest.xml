<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="application.example.food_ordering_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Food_ordering_app">
        <activity android:name=".pnb"></activity>
        <activity android:name=".Rating" />
        <activity android:name=".checkout" />
        <activity android:name=".biryani" />
        <activity android:name=".cart" />
        <activity android:name=".dal_rce" />
        <activity android:name=".Indian" />
        <activity
            android:name=".type_of_food"
            android:label="@string/title_activity_type_of_food"
            android:theme="@style/Theme.Food_ordering_app.NoActionBar" />
        <activity android:name=".login" />
        <activity android:name=".registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>